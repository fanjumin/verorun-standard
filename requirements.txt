flask>=3.0
flask-cors>=4.0
gunicorn>=21.2
PyJWT>=2.8
requests>=2.31
feedparser>=6.0
Pillow>=10.0
APScheduler>=3.10
psycopg2-binary
pydantic>=1.9,<3
openai>=1.0
redis>=5.0
cryptography>=41.0
PyYAML>=6.0
authlib>=1.3
stripe>=7.0
python-dotenv>=1.0
numpy>=1.24.0
edge-tts>=6.0
croniter>=2.0
paramiko>=3.0

# ── Two-Factor Authentication 插件依赖 ──
pyotp>=2.9
qrcode>=7.4
bcrypt>=4.0

# ── Project Workspace 文档解析依赖 ──
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23

# ── Optional: cloud object storage (lazy-loaded by plugins/vault) ──
# S3-compatible storage (AWS / MinIO / 兼容 S3 网关) 需要时取消注释：
# boto3>=1.34
# 阿里云 OSS 需要时取消注释：
# oss2>=2.18
