flask>=3.0
flask-cors>=4.0
gunicorn>=21.2
PyJWT>=2.8
requests>=2.31
feedparser>=6.0
Pillow>=10.0
APScheduler>=3.10
psycopg2-binary
pydantic>=1.9,<3
openai>=1.0
redis>=5.0
cryptography>=41.0
PyYAML>=6.0
authlib>=1.3
stripe>=7.0
python-dotenv>=1.0
numpy>=1.24.0
pandas>=2.0
tushare>=1.4.0
edge-tts>=6.0
croniter>=2.0
paramiko>=3.0

# ── Two-Factor Authentication 插件依赖 ──
pyotp>=2.9
qrcode>=7.4
bcrypt>=4.0

# ── Project Workspace 文档解析依赖 ──
PyMuPDF>=1.24.0
python-docx>=1.1.0
python-pptx>=0.6.23

# ── Optional: cloud object storage (lazy-loaded by plugins/vault) ──
# S3-compatible storage (AWS / MinIO / 兼容 S3 网关) 需要时取消注释：
# boto3>=1.34
# 阿里云 OSS 需要时取消注释：
# oss2>=2.18

# ── ros_bridge 插件依赖（机器人桥接网关；websockets>=12 提供 asyncio 子模块）──
websockets>=12.0

# ── currency_converter 插件依赖（汇率抓取；plugins/currency_converter/requirements.txt 同步声明）──
# 缺失即 setup error: No module named 'httpx'（部署链路只装本文件，不装插件级 requirements）
httpx>=0.27.0,<1.0

# ── Optional: analytics GeoIP（plugins/analytics/geoip.py 内懒导入，缺装仅降级告警不影响主链路）──
# 需要离线 IP 归属地时取消注释，并放置 MaxMind .mmdb 到插件约定目录：
# geoip2>=4.8.0
